# S4(G2) 실배선 판단 기록(S4-EXEC-PLAN E8): 이미지 빌드가 본체(사설) named context를 요구하므로 CI
# 빌드 불가 — digest 기록의 정본 = 캠페인 ⓪ 기계 기록(image-build-record.json·3식별자·CP 매니페스트 결속).
# 본 워크플로는 dispatch 전용 유지 — self-hosted 도입 시 재회부.
name: image-build
on:
  workflow_dispatch: {}
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "S4(G2) 발효 전 자리 — 이미지 빌드 파이프라인은 S4에서 배선된다"

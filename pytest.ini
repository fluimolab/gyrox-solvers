[pytest]
markers =
    m0_assets: requires local immutable Gyrox M0 fixtures and is excluded from the CI subset
    realtime_cancel: real-runtime cancellation timing tests excluded from the CI subset (2-core CI cannot meet the solver timing envelope; judgment canon = campaign make-test full run)
